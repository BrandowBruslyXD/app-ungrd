# Copia este archivo como .env.local (no se commitea) y ajusta si tu backend corre en otra URL.
VITE_API_BASE_URL=http://localhost:5000/api

# Con mocks encendidos la app funciona completa sin backend (valor por defecto: true).
# Ponlo en false para consumir el backend real.
VITE_USAR_MOCKS=true
